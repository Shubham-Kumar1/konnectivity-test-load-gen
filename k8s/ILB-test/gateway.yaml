apiVersion: gateway.networking.k8s.io/v1
kind: Gateway
metadata:
  name: omni-gateway
  namespace: omni-system
spec:
  gatewayClassName: gke-l7-rilb 
  listeners:
  - name: http
    protocol: HTTP
    port: 80
    allowedRoutes:
      namespaces:
        from: All